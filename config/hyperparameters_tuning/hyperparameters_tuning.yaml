defaults:
  - override hydra/hydra_logging: "none"
  - override hydra/job_logging: "custom"

hydra:
  run:
    dir: ./logs/hyperparameters_tuning/${now:%Y-%m-%d}/${now:%H-%M-%S}

test_size: 0.2
random_seed: 2024 # applied for all models
label_col: "price"
initial_build_verbose: 0
features_builder_verbose: 0

model_type: ridge.pkl
metric: "mae"

hyperparameter_tuning:
  name: "my_ridge_high"
  param_grid:
    model:
      alpha: [0]
    ma_clean:
      finalize_flag: [True, False]
    final_ct:
      numerical_encoder: ["passthrough", "standardscaler"]
  cv_no: 10
  random_seed: 2024
  n_jobs: -1
  return_train_score: True
  verbose: 0
  refit: False
